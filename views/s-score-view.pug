include _header.pug
script(src='/js/score.js')
include _nav.pug
.ts.container
	h3.ts.header 基本資料
		.sub.header 確定成績是不是你的（？
	.ts.stackable.three.cards
		mixin card(header,description,icon)
			.ts.card
				.content
					.header= header
					.description= description
				.symbol
					i.icon(class=icon)
		+card('學號',user.id,'qrcode')
		+card('姓名',user.name,'user')
		+card('班級',user.class,'student')
	br
	h3.ts.header 各項成績
		.sub.header 若無法看見完整內容可利用 
			kbd Shift+滾輪
			|  或是手指來移動
	.gs.scroll
		table#score.ts.definition.compact.striped.fixed.single.line.celled.table(style='display:none')
			!= score
	br
	h3.ts.header 總成績
		.sub.header 若無法看見完整內容可利用 
			kbd Shift+滾輪
			|  或是手指來移動
	.gs.scroll
		table#total.ts.definition.compact.striped.fixed.single.line.celled.table(style='display:none')
			!= total
include _footer.pug