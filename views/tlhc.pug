include _header.pug
body
    header.ts.heading.vertically.padded.fluid.slate
        .ts.center.aligned.container
            h1.ts.center.aligned.header!= title
    include _nav.pug
    br
    .ts.container
        .ts.segmented.selection.items
            - var lists = tlhc;
            each item in lists
                a.item(href=item.link)
                    .content
                        .meta.large.device.only
                            span=item.date
                            span=item.tag
                        .header=item.title
        #navigator
            .ts.buttons
                each page in pages
                    a.ts.info.button(href=page.link)=page.text
        br
        br
        br


