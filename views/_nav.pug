.ts.basic.fluid.borderless.menu.horizontally.scrollable
    .ts.container
        a.item(href='/') 公告
        a.item(href='/tlhc/score/') 成績
        .right.fitted.item
            if(score)
                a.item(href='/tlhc/score/logout') 登出
            else
                .ts.underlined.basic.right.icon.input
                    input(type='text', placeholder='搜尋',id='search')
                    i.search.link.icon(onclick="search()")