#nav.ts.basic.fluid.borderless.menu.horizontally.inverted.scrollable
    .ts.container
        if(!user)
            a.item(href='/') 首頁
            a.item(href='/about/') 關於
            a.item(href='/tlhc/login/') 登入
            //-a.item(href='/searchPage/') 搜尋
        else
            a.item(href='/tlhc/score/') 成績
            a.item(href='/tlhc/day/') 出勤
            a.item(href='/tlhc/rewards/') 獎懲
        .right.fitted.item
            if(user)
                a.item(href='/tlhc/score/logout') 登出