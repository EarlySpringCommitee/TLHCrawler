#nav.ts.basic.fluid.borderless.menu.horizontally.inverted.scrollable
    .ts.container
        if(!user)
            a.item(href='/') 首頁
            a.item(href='/about/') 關於
            a.item(href='/tlhc/search/') 搜尋
        else
            a.item(href='#systemMenu') 
                i.icon.sidebar
                |選單
        .right.fitted.item
            if(!user)
                a.item(href='/tlhc/login/') 登入
            else
                a.item(href='/tlhc/score/logout') 
                    i.icon.sign.out
                    |登出
