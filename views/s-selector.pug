include _header.pug
include _nav.pug
.ts.container
    h2.ts.center.aligned.header=header||title
        .sub.header=subheader||'您好，'+user.name
    .ts.doubling.four.cards
        each item in selector
            +selectCard(item.name,item.link,item.icon) 
        else
            .ts.header 未找到符合條件的資料
include _footer.pug