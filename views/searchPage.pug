include _header.pug
include _nav.pug
.ts.very.narrow.container
    .ts.action.fluid.input(style='margin:70px 20px')
        input#search(type='text',placeholder='搜尋')
        button.ts.button(onclick="search()") 搜尋

include _footer.pug